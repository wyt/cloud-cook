spring:
  cloud:
    nacos:
      config:
        prefix: ${spring.application.name}
        file-extension: yml
        group: DEFAULT_GROUP
        namespace: cd9b18a3-f736-434e-84cd-e57259d78228
        server-addr: arch.nacos.mysnippet.io
      discovery:
        server-addr: arch.nacos.mysnippet.io