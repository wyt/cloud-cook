spring:
  application:
    name: app-sample-produce
  cloud:
    config:
      fail-fast: true
      uri: http://config-server:7766  # 配置中心地址
  profiles:
    active: dev

logging:
  path: /applog
  file: "${logging.path}/${spring.application.name}.log"