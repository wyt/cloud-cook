spring:
  cloud:
    nacos:
      config:
        prefix: ${spring.application.name}
        file-extension: yml
        group: DEFAULT_GROUP
        server-addr: arch.nacos.mysnippet.io:80
        username: nacos
        password: nacos
      discovery:
        server-addr: arch.nacos.mysnippet.io:80
        username: nacos
        password: nacos

feign:
  client:
    config:
      default:
        loggerLevel: full
        connectTimeout: 80
        readTimeout: 80